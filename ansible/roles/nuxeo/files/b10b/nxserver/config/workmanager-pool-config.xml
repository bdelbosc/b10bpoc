<?xml version="1.0"?>
<component name="b10b-workmanager-config">
  <require>org.nuxeo.ecm.platform.video.workmanager</require>
  <require>org.nuxeo.ecm.platform.picture.workmanager</require>

  <extension target="org.nuxeo.ecm.core.work.service" point="queues">
    <!-- make video conversion thread pool greater to one to scale on multi node -->
    <queue id="videoConversion">
      <maxThreads>4</maxThreads>
    </queue>
    <!-- Add queue for the storyboard works -->
    <queue id="videoStoryboard">
      <maxThreads>4</maxThreads>
    </queue>

    <!-- create a dedicated pools -->
    <queue id="updateThumbListener">
      <maxThreads>4</maxThreads>
    </queue>

    <queue id="pictureViewsGeneration">
      <maxThreads>4</maxThreads>
    </queue>
  </extension>

</component>
